# Lessons Learned

Append-only — never rewrite or delete an entry; new entries go at the top of `## Entries`. When a `Cause-tag` reaches 3×, **invoke the `writing-lessons` skill** (do not hand-promote): it dispatches an independent promotion review and, on a Promote verdict, authors the rule under `.claude/rules/` via `writing-rules`, then records the cluster in `## Promoted clusters`.

## Entries

### 2026-06-19 — A discipline-gate skill had no observable RED in-vault (baseline already complies); pivoted to the shaping variant, whose failure IS measurable

- **Cause-tag:** `skill-value-vs-noop` (3rd instance)
- **What happened:** Asked to author `pre-implementation-protocol` (readiness check at the `writing-plans → tdd` boundary), modelled on a consumer repo's 6-phase pre-coding protocol. RED on the *discipline* framing ("don't dive into code before verifying the plan"): 4 cold subagents (Opus ×2, Haiku ×2), all under explicit "ignore the operating manual, you are a ship-fast engineer, don't re-litigate" + authority + deadline pressure, STILL reconned, verified the plan's contracts, demanded a green baseline, and refused to code against an unseen interface. No discipline failure reproduced → a gate would have been theatre (same class as the two entries below: strong model + repo discipline already obey the rule). Two confounds compounded it: (1) the proposed phases largely duplicated `framework.md` Implementation Protocol + `writing-specs`/`writing-plans`/`tdd`; (2) subagents spawned via the Agent tool **inherit the vault's `framework.md`**, so the no-skill "control" is contaminated — it complies because the discipline is force-injected globally (agents literally cited "the vault's discipline"). What WAS observable in-vault: high variance of the readiness *output shape* (4 reps → 4 different unstructured reactions: one started Task 1, one blocked, one asked 1 question, one asked 5). That is a *shaping* failure. Pivoted to a positive-recipe shaping skill (fixed 5-slot readiness block, conditional on whether an approved plan exists); GREEN = form convergence — 3 reps incl. Haiku produced the identical block in identical order.
- **Fix / rule:** Classify the baseline failure as **discipline vs shaping BEFORE running RED**. In this vault a discipline-RED via the Agent tool is a contaminated control (inherits `framework.md`) — to measure a true discipline baseline you need an environment without that injection (raw API with a controlled system prompt, or a real consumer repo). Shaping failures (output-shape variance) remain cleanly measurable in-vault and converge under a positive recipe. If the discipline failure can't be reproduced, don't build the gate — find the shaping failure that IS reproducible, or don't write the skill. Cross-refs the `red-scenario-contamination` entry (different mechanism — environmental injection, not prompt-naming — same effect: false "no failure").
- **Prevention:** Before authoring any "protocol/gate/check" skill, name whether its target failure is discipline (rule skipped under pressure) or shaping (behaviour happens, wrong/inconsistent shape). For a discipline target, do NOT trust an in-vault Agent-tool control — its compliance may be inherited `framework.md`; get a clean baseline or pivot. Calibrate GREEN to the reproduced failure, never to the source artifact's stated purpose.
- → promoted to rules/common/scoping-skill-value.md

### 2026-06-19 — Hand-improved the vault's own status block, but the bootstrapping template kept shipping the flat one to consumers

- **Cause-tag:** `fix-instance-not-generator` (2nd instance)
- **What happened:** Across two turns I restructured the vault's own `.claude/CLAUDE.md` end-of-turn Status block from a flat `·`-joined one-liner into a verdict-first, categorized rendered-markdown block. The `bootstrapping-claude-md` operating-manual **template** (`references/operating-manual-template.md`) still shipped the old flat `·`-joined block to every consumer repo it generates. A RED subagent following the stale template reproduced the flat block and, improvising toward scannability, reached for **emoji** — violating the manual's own "no emoji" rule. The instance was dogfooded; the generator lagged. Same class as the `writing-lessons` routing entry below (fixed the CLAUDE.md, not the template).
- **Fix / rule:** When a pattern is improved by hand in the vault's *own* `.claude/CLAUDE.md` (or any artifact that has a generator under `skills/**`), the SAME turn propagate it into the template that ships it. Rewrote the template's Status-block section to the structured, agnostic, verdict-first form (real-check placeholders, no emoji), added a SKILL Red Flag catching the flat-block/emoji drift, and RED→GREEN-confirmed via subagents (RED: flat block + emoji; GREEN: structured, no emoji). Open follow-up: `auditing-claude-md` does not yet flag a flat (non-structured) status block, so it won't catch this drift in a consumer repo.
- **Prevention:** After improving any pattern in the vault's own `.claude/CLAUDE.md`, grep the bootstrapping templates for the same section (`grep -rn "Status block\|<the section>" skills/foundation/bootstrapping-claude-md/`) and fix the template in the same turn; add the inverse check to the matching `auditing-*` skill so the drift is caught in consumer repos too.

### 2026-06-19 — `resolving-requirements` shipped two criteria whose literal text was unfollowable; only RED subagents exposed that compliance depended on overriding the text

- **Cause-tag:** `unfollowable-literal-criterion`
- **What happened:** Validating `resolving-requirements`, two rules read fine but were defective when taken literally. (1) Step 2 "First non-empty match wins" is a **positional** tie-breaker — filesystem enumeration order picks the source, and it would justify selecting a lossy one-line `ABC-12.md` over the fuller `ABC-12/` directory bundle (silent requirement loss + non-determinism). (2) The Output contract's "carry source content **verbatim**" is **unsatisfiable for a binary** attachment (a `wireframe.png` listed in Step 3's "read every file … mockups"): a PNG has no verbatim text, so the criterion invites describing the image — authored, not sourced, requirements, the exact failure the skill exists to prevent. Both defects were invisible to structural validators (frontmatter/fences/links/word-count all passed). Two RED subagents on the current text *recovered* — but only by explicitly overriding the literal instruction ("reads as purely positional and would, taken literally, justify selecting the lossy file — a real bottleneck"; "not literally satisfiable by transcription"). A weaker agent following the text verbatim would pick the lossy file / fabricate a mockup description.
- **Fix / rule:** A skill rule/completion-criterion must be followable *as written* — predictability fails when compliance depends on the agent silently reinterpreting the text. Fixed Step 2 to an order-independent rule ("prefer the fullest match; genuinely equivalent → surface and ask, never let order decide") and the contract to name the non-text exception ("a non-text attachment has no verbatim text: carry it by reference — name + path — never transcribe or describe its content as source"). Added matching Red Flags + Rationalization rows. GREEN-confirmed: both scenarios now comply straight from the literal text. (Structural validators alone are insufficient — they never exercise behaviour; this is why a GREEN subagent run is a separate Completeness-Checklist row.)
- **Prevention:** When authoring/validating a skill, scan every tie-breaker and every "verbatim/exhaustive/all" criterion: a tie-breaker that resolves by position/order is non-deterministic — rephrase to a content-property the order can't change; a "verbatim/exhaustive" criterion must name its binary/non-text exception or it's unsatisfiable on the first attachment. Don't trust a clean structural-validator pass as behavioural proof — RED a literal-reading scenario before calling it done.

### 2026-06-19 — Authoring a new skill, asserted a neighbour skill's hand-off capability (frontmatter/trace-back) without reading it

- **Cause-tag:** `wrong-assumption` (2nd instance)
- **What happened:** Writing the new `resolving-requirements` skill, its Output-contract section claimed provenance "travels down the chain so `writing-specs` can stamp it into the SPEC frontmatter and `spec-drift-audit` can trace back to the exact requirements" — a structural claim about TWO neighbour skills written from expectation, never read. The owner challenged it ("я не уверен на счёт этого"). On a `grep`: `writing-specs` has no frontmatter/provenance field at all (its 8 required sections start at Goal), and `spec-drift-audit` traces code↔spec by spec path, not spec↔source. The claim was fabricated; only the owner caught it, not my own read-before-assert. The original FLIBCO request text said "record in SPEC frontmatter", which I carried over as if the vault's `writing-specs` implemented it.
- **Fix / rule:** Same class as the `detect-bypass.sh` entry below — do not characterize another component (hook OR skill) from its name, your expectation, or a source request's wording. A hand-off claim FROM a new skill INTO an existing one names the receiving skill's *real* capabilities only after reading that skill THIS session. Softened the text to keep provenance a property of the bundle and to state explicitly that `writing-specs`/`spec-drift-audit` define no such slot (a downstream slot would be a separate, tested change to those skills).
- **Prevention:** When a skill/rule you are authoring asserts what a *neighbour* skill does (stamps X, consumes Y, traces Z), `grep`/`Read` that neighbour's `SKILL.md` for the named mechanism before writing the sentence; if grep returns nothing, the mechanism doesn't exist — describe the hand-off without inventing the receiver's behaviour.

- **Cause-tag:** `human-handoff-command-fragility`
- **What happened:** The hook-migration plan had a `[HUMAN]`-run step: `git mv` 12 files + run the linker. I gave the human a multi-line, `\`-continued `&&` chain to paste after `!`. First paste fired only `git mv .claude/hooks` (continuation lost → usage error); the single-physical-line `&&` retry hit `zsh: parse error near &&` (paste truncation again). Two wasted round-trips. Only when I wrote the steps into `scripts/migrate-hooks.sh` and had the human run `!bash scripts/migrate-hooks.sh` (one short token) did it work first try.
- **Fix / rule:** A command handed to the human for `!`-execution must be a SINGLE short line with no `\` continuations and no long `&&` chains — terminal paste mangles multi-line and over-long lines. For anything beyond one trivial command, write a throwaway script (Claude can `Write` it even when the commands themselves are guard-blocked for Claude to *run*) and hand the human a one-token `bash scripts/<x>.sh`. Delete the scratch script after.
- **Prevention:** In `writing-plans`, when a step is `[HUMAN]`-run and has >1 command, emit it as a script-file + a one-line invocation, never as a pasteable multi-line block. Grep the plan for `[HUMAN]` steps containing `&&` or a trailing `\` — rewrite each to a script invocation.

### 2026-06-19 — `git mv` + symlink at the IDENTICAL old path registers as add+typechange, not a rename (history check needs a blob compare)

- **Cause-tag:** `git-symlink-shadows-rename`
- **What happened:** Verifying "history preserved" after the hook move, `git diff --cached --find-renames` returned ZERO renames and `git status` showed ` T .claude/hooks/<name>.sh` (typechange). Alarming — the skills migration had shown clean `R100` renames. Root cause: for skills the moved unit was a *directory* and the symlink took a *new* leaf path, so the old file path was deleted → git paired it as a rename. For hooks the symlink occupies the **exact same path** as the old file (`.claude/hooks/<name>.sh`), so the old path is not deleted — it becomes a symlink (mode 120000) — and git sees "new file added under `hooks/` + old path typechanged to symlink", which rename detection does not pair. History is still intact: `git rev-parse HEAD:.claude/hooks/<name>.sh` == `git rev-parse :hooks/<cat>/<name>.sh` (identical blob `3cee0a7…`), so `git log --follow` traces it by content.
- **Fix / rule:** "No `R` in `git status`" does NOT mean history was lost. When a migration places a symlink at the same path a real file used to occupy, verify history by **blob-identity** (`git rev-parse HEAD:<old>` == `git rev-parse :<new>`), not by rename detection. The end state (real file added at new path, old path staged as a 120000 symlink) is correct and committable.
- **Prevention:** For any "move + symlink-back" migration, pick the history check by shape: dir-move with symlink at a new name → expect `R100`; file-move with symlink at the identical old path → expect add+typechange, confirm via blob-equality instead.

### 2026-06-19 — Patching docs for a layout change, baked the volatile `<category>` level into 7 places where only the stable invariant matters

- **Cause-tag:** `doc-over-coupled-volatile-detail`
- **What happened:** Updating the docs for the categorized-skill-layout migration, I wrote `skills/<category>/<name>/` (and once enumerated all five category names, and once injected a full path into a *role-level* layer reference — `framework.md` "the layers of a skill change") across 7 locations: glossary row #1, `framework.md` L10, `skill-routing-sync.md` L22+L62, root `CLAUDE.md` L19, `.claude/CLAUDE.md` L17+L32. But `<category>` is the one dimension the design explicitly made **volatile** — cheap to re-categorize, deliberately NOT leaked into routing. The stable invariant is `skills/` + the flat symlink `.claude/skills/<name>`, with `name === leaf dir === symlink === routing key` (category does not participate). Coupling docs to the volatile level creates drift debt: re-categorize a skill and these cells/sentences go stale. The owner caught it twice (framework, then glossary, then skill-routing-sync) before I swept the whole class.
- **Fix / rule:** When patching docs for a structural change, reference the **stable invariant, not the volatile dimension**, and match the prose's existing abstraction level — a layer-*role* reference ("SKILL.md frontmatter") carries no path; a location cell uses `skills/` (or a `skills/*/<name>/` glob), never the specific taxonomy level; never enumerate a taxonomy designed to change. Swept all 7 to `skills/` / `skills/*/<name>/`.
- **Prevention:** After a layout-driven doc edit, grep the volatile token across **all** edited docs in one pass (`grep -rn '<category>' <edited docs>`); any hit in a cell/sentence not specifically about that dimension is over-coupling — strip to the stable form then, not after the owner flags each file. Fix the whole class at once.

### 2026-06-19 — Claude Code follows symlinked skill directories for discovery (undocumented — verified, not assumed)

- **Cause-tag:** `undocumented-platform-behavior`
- **What happened:** Reorganizing the vault's 15 skills into category folders (`skills/<category>/<name>/`) with flat symlinks `.claude/skills/<name>` → `../../skills/<category>/<name>`, the load-bearing question was whether Claude Code discovers a skill through a **symlinked** directory. The official docs (code.claude.com) cover symlinks only for *rules*, and say nothing about symlinks for *skills* — readiness-review flagged this as an unverified go/no-go. Resolved empirically: with the real skill at `skills/apply-chain/grilling/` and a `mode 120000` symlink at `.claude/skills/grilling`, a subagent in a fresh context invoked `Skill(grilling)` and the body loaded (reporting its base dir as the symlink path). Discovery follows the symlink transparently.
- **Fix / rule:** When a design rests on platform behavior the vendor docs do NOT document, do not proceed on "it probably works" — run a minimal empirical probe (here: one symlinked skill + a fresh-context `Skill` invocation) before building the full migration on it. Also neutralize the risk *structurally* where possible: variant B keeps `.claude/skills/` containing **only** symlinks (no category dirs), so even if discovery were recursive there is no second copy to double-register — the uncertainty is engineered out, not just tested around.
- **Prevention:** For any change whose correctness depends on undocumented tool/platform behavior, add a one-shot probe step to the plan that reproduces the exact mechanism in isolation, and prefer a structure that is correct under both possible behaviors. Treat "docs are silent" as "must verify", never as "safe by default".

### 2026-06-19 — Reference-link validator regex flagged an illustrative inline example as a broken link (false positive)

- **Cause-tag:** `self-check-format-drift` (2nd instance)
- **What happened:** During final acceptance of the skill-layout migration, an ad-hoc reference-link validator (`grep -oE '\]\(([^)]+\.md)\)'`) reported `BROKEN-LINK: skills/authoring/writing-rules/SKILL.md -> ./error-handling.md`. Investigation: that string is an **illustrative inline example** of the cross-linking pattern in the skill's prose — `Cross-link siblings with relative links (`[error-handling](./error-handling.md)`)` — not a navigational link the skill relies on. The file never existed; the "link" was present in HEAD, so the migration neither introduced nor broke anything. The validator gave a wrong verdict by not distinguishing a demonstrated-pattern example from a real link.
- **Fix / rule:** Same family as the promotion-debt scan drift below: a verification-step regex over markdown must account for real markdown structure. A reference-link checker must exclude links that are illustrative — inside backtick inline spans, inside fenced example blocks, or explicitly marked illustrative — before declaring a broken link, or it produces false positives that erode trust in the check. Did NOT "fix" the writing-rules example (pre-existing, out of scope for this migration — like the `improve-codebase-architecture` routing gap); recorded it instead.
- **Prevention:** When writing/automating the vault's "reference links resolve" validator, skip any `](…)` occurrence whose surrounding context is a backtick inline span or a fenced block; only treat top-level prose links as navigational. Re-confirm a flagged "broken link" by reading its line before acting on it.

### 2026-06-19 — Fixed the CLAUDE.md instance but not the template that generates it (bootstrapping kept reproducing the bug)

- **Cause-tag:** `fix-instance-not-generator`
- **What happened:** After fixing the vault's own `.claude/CLAUDE.md` to route lesson capture through the `writing-lessons` skill, the `bootstrapping-claude-md` operating-manual **template** still said "append it to lessons-learned.md the SAME turn" (Non-negotiable #5) with the skill only as an optional angle-bracket placeholder. Every CLAUDE.md generated from it would have shipped the just-fixed bypass. The instance was fixed; the generator was not.
- **Fix / rule:** When you fix a defect in an artifact that is produced from a template/generator (a CLAUDE.md, a scaffolded file, a rule), the SAME turn check the generator for the same defect and fix it there — otherwise the fix is local and the bug regenerates. Propagated the routing rule into `bootstrapping-claude-md` (template #5 + Lessons path + a third "overrides the templates' defaults" rule) and added the inverse-drift check to `auditing-claude-md`. Kept it agnostic/conditional ("if the repo has a lessons-capture skill"), matching the template's existing `handoff` precedent.
- **Prevention:** On any fix to a file that has a generator/template in `.claude/skills/**` (grep the skill set for the artifact name or a template producing it), apply the fix to the template too, and add/confirm an audit check that catches the drift; a RED/GREEN on the *generated* output, not just the hand-fixed instance.

### 2026-06-19 — `writing-lessons`'s own promotion-debt scan silently matched nothing (format drift)

- **Cause-tag:** `self-check-format-drift`
- **What happened:** The scan command documented in `writing-lessons/SKILL.md` was `grep -oE 'Cause-tag\*\*:…'` — it matches `Cause-tag**:` (template format), but the real entries are written `**Cause-tag:**` with the tag in backticks, and the file's intro prose says `` `Cause-tag` ``. Run literally on the live log it returned **empty** → a false "no promotion debt", so a cluster could cross the threshold of 3 unnoticed. Three cosmetic formats of one field had drifted apart and the self-check matched none of the actual entries.
- **Fix / rule:** A field-scraping regex in a skill's own verification step must be anchored to the field's stable structural marker and tolerant of cosmetic markdown variance — not pinned to one punctuation layout. Fixed to anchor on the **list-item line start** (`^[[:space:]]*-[[:space:]]+\*\*Cause-tag[^[:alnum:]]+[a-z0-9-]+`): absorbs colon-inside/outside and optional backticks, and the line anchor excludes prose that merely quotes the field (a body-only anchor still matched a lesson that discussed `Cause-tag` in its Fix text — a real false positive caught only because the log itself now contains such prose).
- **Prevention:** Author/verify any "run this to self-check" command against the REAL artifact at write time, never assume it matches the template; for markdown field scrapes, anchor on the field's **line-start list marker** (not just the bold span, which prose can quote) and allow `[^[:alnum:]]+` between marker and value.

### 2026-06-19 — A RED scenario that foregrounds tool choice contaminates the test and yields a false GREEN

- **Cause-tag:** `red-scenario-contamination`
- **What happened:** Diagnosing the `writing-lessons` bypass, the first RED prompt asked the subagent to "state the exact first tool call you would make". It complied via `Skill` — a false GREEN. The premise (the agent writes the lesson via direct `Edit`) only reproduced when the scenario was naturalistic: a real task with the capture incidental and **no tool named**, where the agent ran `Read→Read→Edit`, bypassing the skill. Same agent, same manual — only the prompt's framing differed.
- **Fix / rule:** A RED/baseline subagent prompt must never name, or ask the agent to reason about, the tool/skill under test — that cue is the thing you are trying to observe emerge (or not) on its own. Give a realistic task, let the behaviour happen, and read it from a post-hoc tool list or `git diff`, not from a "which tool would you use" question. This is `writing-great-skills`' inversion test applied to the *scenario*: if foregrounding the choice flips the result, the scenario, not the skill, decided it.
- **Prevention:** Before running a baseline, grep your own scenario prompt for the tool/skill name under test (`Skill`, `Edit`, the skill's name) — any hit in the instruction is contamination; strip it and re-run.

### 2026-06-19 — `detect-bypass.sh` has three distinct warning conditions, not just "Read a skill body"

- **Cause-tag:** `wrong-assumption`
- **What happened:** A question asserted the bypass hook "only ever warns when you Read a skill's SKILL.md body". Reading [hooks/detect-bypass.sh](hooks/detect-bypass.sh) showed three separate `SKILL-BYPASS warn:` paths: **(1)** `Read` of a path registered as a skill's `files` body without the matching `Skill` invoked this turn (lines 56–77); **(1b)** `Edit`/`Write`/`MultiEdit` to `lessons-learned.md` without invoking `writing-lessons` first (lines 79–91); **(2)** a once-per-turn threshold warning — after `TRIGGER_BYPASS_THRESHOLD=3` non-Skill tool calls, if the stored user prompt matches a skill's `triggers` regex and that skill was never invoked (lines 93–121). Conditions (1)/(1b) fire per matching tool call; (2) is gated by a counter + a one-shot `turn-bypass-warned.flag`.
- **Fix / rule:** Don't characterize a hook's behavior from its name or a single code path — read every `echo … >&2` / warn branch before stating "it only does X". Confirmed by reading all three branches this turn.
- **Prevention:** When asked "does hook X only do Y?", grep the hook for every warn/exit branch (`grep -nE 'warn|>&2|exit' hook.sh`) and enumerate each condition before answering.

### 2026-06-19 — "Add a verify/review phase" defaults to a no-op; the binding value is the efficacy test, not another correctness pass

- **Cause-tag:** `skill-value-vs-noop` (2nd instance)
- **What happened:** Asked to add a "Review phase" to `writing-rules` that tests a freshly-written rule "meets all conditions". The literal reading is a *correctness* review. RED first: 3 escalating baseline scenarios (swapped ✅/❌, `paths`-glob miss, dead-path reference) were ALL caught by a cold agent on the current skill — the strong model + the repo's read-before-assert discipline already cover reading-detectable defects. A correctness phase would have been a `writing-great-skills` no-op (same root cause as the entry below: principle/correctness content a strong model already obeys).
- **Fix / rule:** When asked to add a "verify/review/validate" phase, RED-test the *literal* version against the baseline before building. If the baseline catches it, the non-no-op addition is the **efficacy** test, not another correctness pass — for a rule, a cold-agent RED/GREEN ("does the rule actually steer behaviour?"). Built that instead; GREEN-confirmed by catching a well-formed `const`-vs-`var` rule a cold agent complies with by default (a no-op rule static review approves). Landed as `writing-rules` → "Test the rule on a cold agent" + `references/rule-efficacy-test-prompt.md`.
- **Watch:** this is the 2nd `skill-value-vs-noop` instance — one more → promote to a rule on scoping skill/phase value to reproduced failures.
- → promoted to rules/common/scoping-skill-value.md

### 2026-06-19 — A skill's "principles" are a no-op for a strong model; the binding value is lexicon + deliverable shape + skipped process steps

- **Cause-tag:** `skill-value-vs-noop`
- **What happened:** RED baseline for migrating `codebase-design` / `improve-codebase-architecture` — 4 cold subagents on well-framed design prompts reached the skills' *principles* unaided (deletion-test reasoning, deep/shallow detection, inject-don't-create, fakes>mocks, "no seam until two adapters", domain-shaped narrow ports). A skill teaching those to a strong model proves little (inversion test: it complies WITHOUT the skill). What it reliably FAILED, 4/4: vocabulary drift (every agent wrote the explicitly-forbidden "boundary", plus "layers/wrappers/shell/core" — no fixed lexicon), no structured visual deliverable when asked to "present findings", skipped the explore→present→grill process, and produced ONE interface design instead of design-it-twice.
- **Fix / rule:** Calibrate GREEN to the *reproduced* failure, not the source's stated purpose. For these skills the defensible value = enforced shared vocabulary + the structured deliverable + the process steps a model skips — keep those load-bearing, treat principle-prose as light scaffolding. Vocabulary drift is a *shaping* failure → fixed-lexicon recipe ("use exactly X; never substitute Y"), not prohibitions (per `writing-great-skills` "Match the Form to the Failure").
- **Watch:** if this recurs (a third skill whose principle-content is no-op for strong models) → promote to a rule on scoping skill value to reproduced failures.
- → promoted to rules/common/scoping-skill-value.md

### 2026-06-19 — Naive fence-toggle corrupts markdown-in-markdown when auto-fixing

- **Cause-tag:** `markdown-fence-counting`
- **What happened:** A bulk fixer that added a language to "bare opening fences" treated every ` ``` ` as a toggle. In template files that wrap example fenced blocks in a four-backtick fence (` ````markdown ` … ` ```` `), the inner three-backtick fences are literal content; naive toggling desynced and appended `text` to *closing* fences.
- **Fix / rule:** Parse fences per CommonMark — a fence opened with N backticks closes only on a line of ≥N backticks with no info string; inner shorter fences are content. Same rule for skipping tables inside example blocks. Codified in [rules/common/markdown-style.md](rules/common/markdown-style.md) (Fenced-code bullet + Edge Cases).
- **Also:** Python `glob('**/*.md')` skips dot-directories (`.claude/`) — use `os.walk` for repo-wide markdown sweeps.

## Promoted clusters

- skill-value-vs-noop → rules/common/scoping-skill-value.md (2026-06-19)
