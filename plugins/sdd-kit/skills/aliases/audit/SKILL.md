---
name: audit
description: Short user-typed entry point to check shipped code against an approved spec. Alias for the verifying-implementation skill.
disable-model-invocation: true
argument-hint: "<spec path or area to audit, optional>"
---

Use the `verifying-implementation` skill. Treat the input below as the spec or area to
audit the shipped code against.

Input: $ARGUMENTS
