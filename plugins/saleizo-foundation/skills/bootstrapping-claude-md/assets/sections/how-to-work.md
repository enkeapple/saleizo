## How to work here (read first)

Every non-trivial task runs through **the SDD chain** (`grilling → writing-specs → writing-plans → pre-implementation-protocol → (inline-driven-development | subagent-driven-development) → verifying-implementation`, each task test-first via `test-driven-development`) and ends with the Completeness Checklist walked row by row in the status block. Front door: `sdd-lifecycle` (`<front-door-command, e.g. /lifecycle>`). Full operating manual: [`<operating-manual filename>`](./`<operating-manual filename>`).
