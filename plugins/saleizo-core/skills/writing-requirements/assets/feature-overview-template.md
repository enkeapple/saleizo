---
status: draft
---

# <Feature name>

*(the feature overview — `requirements.md` at the root of a `features/YYYY-MM-DD-<slug>/` folder; each story lives in its own `NN-<story-slug>.md` beside it. This overview's `status:` is the feature's overall lifecycle state — `draft → ready → in progress → done`, starting at `draft`; each story file carries its own `status:` so stories progress independently. See the skill's Lifecycle status section.)*

> **Source ticket:** <ticket ID or "free-text idea">
> **Depends on:** <prerequisite work, or "none">

## Context

<What this feature covers and why, in business terms. Any phasing or dependency notes go here — NOT in a story's "so that" clause.>

## Stories

| # | Story | Theme | File |
| --- | --- | --- | --- |
| US1 | <title> | <one-line theme> | `01-<slug>.md` |
| US2 | <title> | <one-line theme> | `02-<slug>.md` |

*(link each File cell to the real story file once created)*

## Glossary

*(business terms the stories rely on, so every reader reads the acceptance criteria the same way)*

| Term | Meaning |
| --- | --- |
| <term> | <plain-business definition> |

## Shared Supporting Details

*(cross-cutting tables referenced by more than one story — timing/config parameters, shared state tables, deltas from the current system. Story-specific tables stay in that story's file.)*

### <Shared table title>

| … | … |
| --- | --- |

## Open Questions

*(feature-wide questions; a question that only concerns one story lives in that story's file)*

| # | Question |
| --- | --- |
| 1 | <specific decision needing confirmation> |
