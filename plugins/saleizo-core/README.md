# SDD Kit

The gated spec-driven-development chain — turn a fuzzy idea into shipped, spec-checked code one approved step at a time — plus `systematic-debugging` and `handoff`. Authoring, alias, and framework-setup skills now live in their own plugins (`saleizo-authoring`, `saleizo-commands`, `saleizo-foundation`).

## Quickstart

1. Install it — see [Installation](#installation).
2. Run the chain on a change: `grilling` (design) → `writing-specs` → `writing-plans` → `pre-implementation-protocol` → execute test-first (`test-driven-development` / `inline-driven-development` / `subagent-driven-development`) → `verifying-implementation`. Or drive the whole thing with `sdd-lifecycle`.
3. Short aliases for the common entries ship in `saleizo-commands`: `/grill`, `/spec`, `/sdd`, `/audit`.

## How it works

Each phase is its own skill and ends at an explicit approval gate — nothing auto-advances. A skill hands its artifact (design → spec → plan) to the next, so a weak hand-off between two steps is visible instead of silent. `systematic-debugging` is a standalone entry that drives a bug to a confirmed root cause before handing the fix to `test-driven-development`; `handoff` persists and resumes work across a context limit.

## Installation

### Claude Code

```text
/plugin marketplace add enkeapple/saleizo
/plugin install saleizo-core@saleizo
```

## What's Inside

- **the chain** — `resolving-requirements` → `grilling` → `writing-specs` → `writing-plans` → `pre-implementation-protocol` → execute (`test-driven-development` / `inline-driven-development` / `subagent-driven-development`) → `verifying-implementation`, each phase gated and test-first.
- **sdd-lifecycle** — the front door that classifies where a change enters the chain and drives it gate by gate.
- **systematic-debugging** — investigate a bug to a confirmed root cause, then hand the fix to test-first.
- **handoff** — persist and resume work across a context limit.

## Skills

<!-- skills:start -->
<!-- Generated by bootstrapping-readme — do not edit by hand; rerun the skill. -->

- **[resolving-requirements](skills/resolving-requirements/SKILL.md)** — Use at the very start of the chain, before grilling, when the build input is a ticket reference rather than a ready description.
- **[grilling](skills/grilling/SKILL.md)** — Use before any creative or build work — a new feature, component, behavior change, or refactor — to turn a fuzzy idea into a shared, concrete design.
- **[writing-specs](skills/writing-specs/SKILL.md)** — Use to turn an approved design or written requirements into a concrete spec — reached from `grilling`'s handoff, or directly when continuing a half-finished feature.
- **[writing-plans](skills/writing-plans/SKILL.md)** — Use when you have an approved spec or written requirements for a multi-step task and are about to implement.
- **[pre-implementation-protocol](skills/pre-implementation-protocol/SKILL.md)** — Use at the plan-to-implementation boundary, right before the first edit of a planned change.
- **[test-driven-development](skills/test-driven-development/SKILL.md)** — Use when writing implementation code test-first — a bugfix, behavior change, or the build step of a planned feature.
- **[inline-driven-development](skills/inline-driven-development/SKILL.md)** — Use when executing an approved implementation plan yourself, solo, in the current session — no subagent dispatch.
- **[subagent-driven-development](skills/subagent-driven-development/SKILL.md)** — Use when an approved multi-task implementation plan exists and you want to execute it with isolated context and review gates per task.
- **[systematic-debugging](skills/systematic-debugging/SKILL.md)** — Use when a bug, test failure, crash, or unexpected behavior appears and the cause is not yet known — investigate to a confirmed root cause before any fix.
- **[verifying-implementation](skills/verifying-implementation/SKILL.md)** — Use when checking whether shipped or in-progress code still matches an approved written spec.
- **[sdd-lifecycle](skills/sdd-lifecycle/SKILL.md)** — Use when the user wants the full spec-driven pipeline run end-to-end on a feature or change.
- **[handoff](skills/handoff/SKILL.md)** — Use when a task crosses the plan-file threshold, or when a turn ends incomplete and work must resume in a fresh session.

<!-- skills:end -->

## Philosophy

- **Gates over momentum.** Every phase stops for explicit approval; the chain never races ahead of agreement.
- **Test-first, tiered by edit-type.** A behavioral skill or skill edit needs a failing subagent run (RED) first; a descriptive edit cites a real prior failure; a mechanical fix runs validators only.
- **Agnostic by default.** The chain bakes in no stack or paths; the consumer repo supplies them.
- **Surface the bottleneck.** A leaky hand-off or an over-rigid step is the bug, and it becomes the next lesson and rule.

## Contributing

Author or change a skill through `writing-skills` (in `saleizo-authoring`; RED → GREEN → REFACTOR → VALIDATE). A change is done only when the validators pass and an independent subagent run confirms the behavior. Regenerate this catalog with `bootstrapping-readme`; check it for drift with `auditing-readme` (both in `saleizo-foundation`).
